#include <stdio.h>
#include <float.h>

int main() {
    float numero_maximo = FLT_MAX;
    float suma = numero_maximo;
    
    printf("El máximo número flotante que se puede mostrar en una suma es: %e", suma);
    
    return 0;
}
